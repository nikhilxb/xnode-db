/** Action type definitions. */
export const SymbolTableActions = {
    GET_SYMBOL_SHELL:  "SYMBOLTABLE::",
    GET_SYMBOL_SHELL:  "SYMBOLTABLE::",
};

/** Action creator to __. */
export function getSymbolShellAction(symbolId) {
    return {
        type: SymbolTableActions.GET_SYMBOL_SHELL,

    };
};